import { setupCreators } from "#base";

export const { createCommand, createEvent, createResponder } = setupCreators({
    commands: {
        verbose: true,
        guilds: ["926853877029036102","1121799167405207612"]
    }
});
